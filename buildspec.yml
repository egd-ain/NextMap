version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
  pre_build:
    commands:
      # Setup SSH deploy keys
      - npm install
  build:
    commands:
      - pwd
      - ls

artifacts:
  files:
    - appspec.yml
    - git-deploy.sh